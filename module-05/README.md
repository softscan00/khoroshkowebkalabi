# Модуль 5: Функции (declaration, expression, arrow)

## 🎯 Цель модуля

После прохождения модуля вы узнаете три способа создания функций в JavaScript и поймете, когда какой способ лучше использовать.

---

## 📚 Теория

### Что такое функция?

**Функция** — это блок кода, который можно вызывать многократно. Функции позволяют:
- Не повторять один и тот же код
- Разбивать программу на логические части
- Переиспользовать код в разных местах

### Зачем нужны разные способы?

В JavaScript есть три способа создать функцию. Каждый имеет свои особенности, и понимание различий поможет писать более чистый код.

### Три способа создания функций

#### 1. Function Declaration (объявление функции)

```javascript
function greet(name) {
  return `Привет, ${name}!`;
}
```

**Особенности:**
- Начинается с ключевого слова `function`
- Имеет имя (здесь `greet`)
- **Hoisting** — можно вызывать до объявления в коде

```javascript
sayHello(); // Работает!

function sayHello() {
  console.log('Привет!');
}
```

#### 2. Function Expression (функциональное выражение)

```javascript
const greet = function(name) {
  return `Привет, ${name}!`;
};
```

**Особенности:**
- Функция присваивается переменной
- Нет hoisting — нельзя вызвать до объявления
- Точка с запятой в конце (как у любого присваивания)

```javascript
sayHello(); // Ошибка! Cannot access before initialization

const sayHello = function() {
  console.log('Привет!');
};
```

#### 3. Arrow Function (стрелочная функция)

```javascript
const greet = (name) => {
  return `Привет, ${name}!`;
};
```

**Сокращенный синтаксис:**
```javascript
// Один параметр — скобки можно опустить
const double = n => n * 2;

// Несколько параметров — скобки обязательны
const add = (a, b) => a + b;

// Без параметров — пустые скобки обязательны
const getRandom = () => Math.random();
```

**Особенности:**
- Самый короткий синтаксис
- Если тело функции — одно выражение, `return` можно опустить
- Нет своего `this` (важно для продвинутых тем)

---

## 💡 Примеры

### Пример 1: Три способа написать одну функцию

```javascript
// Function Declaration
function add1(a, b) {
  return a + b;
}

// Function Expression
const add2 = function(a, b) {
  return a + b;
};

// Arrow Function
const add3 = (a, b) => a + b;

// Все три работают одинаково
add1(2, 3); // 5
add2(2, 3); // 5
add3(2, 3); // 5
```

### Пример 2: Короткие стрелочные функции

```javascript
// Удвоение числа
const double = n => n * 2;
double(5); // 10

// Проверка на четность
const isEven = n => n % 2 === 0;
isEven(4); // true

// Получение длины строки
const getLength = str => str.length;
getLength('hello'); // 5
```

### Пример 3: Функция с несколькими строками

```javascript
const calculateDiscount = (price, discount) => {
  const discountAmount = price * discount / 100;
  const finalPrice = price - discountAmount;
  return finalPrice;
};

calculateDiscount(1000, 20); // 800
```

---

## ✏️ Задание

### Описание

Создайте три функции для математических операций, каждую своим способом:

1. **`double`** — удваивает число (Function Declaration)
2. **`triple`** — утраивает число (Function Expression)
3. **`square`** — возводит в квадрат (Arrow Function)

### Требования

- [ ] `double` — объявлена через Function Declaration
- [ ] `triple` — объявлена через Function Expression
- [ ] `square` — объявлена через Arrow Function
- [ ] Все функции принимают один параметр (число)
- [ ] Все функции возвращают результат вычисления

### Примеры использования

```javascript
double(5);  // 10
double(0);  // 0
double(-3); // -6

triple(4);  // 12
triple(0);  // 0
triple(-2); // -6

square(3);  // 9
square(0);  // 0
square(-4); // 16
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Вспомните, чем отличаются три способа объявления функций в JavaScript. Какой из них начинается с ключевого слова `function` и имеет имя? Какой присваивается переменной? А какой использует символ `=>`?

</details>

<details>
<summary>Подсказка 2: Структура</summary>

Каждая из трёх функций принимает одно число и возвращает результат простой арифметической операции. Разница только в **способе объявления**: первая — через ключевое слово `function` с именем, вторая — через присваивание анонимной функции в переменную (`const`), третья — через стрелочный синтаксис (`=>`). Вспомните, как выглядит каждый из этих способов из раздела теории.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

Функция `double` объявляется как Function Declaration, принимает число `n` и возвращает его, умноженное на 2. Функция `triple` объявляется как Function Expression (присваивается в `const`), принимает `n` и возвращает произведение `n` на 3. Функция `square` объявляется как Arrow Function и возвращает `n`, умноженное само на себя. Стрелочную функцию с одним выражением можно записать без фигурных скобок и `return`.

</details>

---

## 🧪 Как проверить решение

1. Запустите тесты для этого модуля:
   ```bash
   npx vitest module-05/index.spec.js
   ```

2. Или запустите UI для визуальной проверки:
   ```bash
   npm run test:ui
   ```

---

## 📖 Дополнительные материалы

- [MDN: Функции](https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Functions)
- [MDN: Стрелочные функции](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Functions/Arrow_functions)
- [JavaScript.info: Функции](https://learn.javascript.ru/function-basics)
- [JavaScript.info: Стрелочные функции](https://learn.javascript.ru/arrow-functions-basics)

---

## ❓ Частые вопросы

**Q: Какой способ лучше использовать?**
A: Для большинства случаев подходят стрелочные функции — они короче. Function Declaration удобна, когда нужен hoisting. Function Expression используется реже, но полезна для понимания.

**Q: Что такое hoisting?**
A: Hoisting — это "поднятие" объявлений функций наверх области видимости. Благодаря этому Function Declaration можно вызывать до её объявления в коде.

**Q: Когда нельзя использовать стрелочные функции?**
A: Стрелочные функции не подходят для методов объектов (из-за отсутствия своего `this`) и конструкторов. Но это продвинутые темы, пока можно не беспокоиться.

**Q: Зачем точка с запятой после Function Expression?**
A: Потому что это присваивание переменной (`const x = ...;`), а присваивания заканчиваются точкой с запятой.

---

## 🎓 Что дальше?

После выполнения этого модуля вы завершили **Блок 1: Основы JavaScript**! Переходите к **Модуль 6: Методы массивов (push, pop, shift, unshift)**, где вы научитесь добавлять и удалять элементы из массивов.
