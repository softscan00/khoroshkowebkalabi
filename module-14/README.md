# Модуль 14: Event Loop и setTimeout

## 🎯 Цель модуля

Понять, как JavaScript выполняет код асинхронно: что такое Event Loop, Call Stack и как работает setTimeout.

---

## 📚 Теория

### JavaScript — однопоточный

JavaScript выполняет код **в одном потоке** — то есть одну операцию за раз. Но при этом он умеет работать с асинхронными задачами (таймеры, запросы к серверу). Как это возможно?

### Call Stack (Стек вызовов)

**Call Stack** — это структура данных, которая отслеживает выполнение функций. Работает по принципу LIFO (Last In, First Out — последний вошёл, первый вышел).

```javascript
function multiply(a, b) {
  return a * b;
}

function square(n) {
  return multiply(n, n);
}

console.log(square(4));
```

```
Стек вызовов:
1. [console.log(square(4))]     // вызываем console.log
2. [square(4)]                   // square вызывает multiply
3. [multiply(4, 4)]              // multiply выполняется
4. [square(4)]                   // возвращаемся в square
5. [console.log(16)]             // выводим результат
6. []                            // стек пуст
```

### Event Loop (Цикл событий)

**Event Loop** — механизм, который позволяет JavaScript выполнять асинхронный код, не блокируя основной поток.

```
┌───────────────────────────────────────────────────────────┐
│                      Call Stack                            │
│                  (синхронный код)                          │
└───────────────────────────────────────────────────────────┘
                            ▲
                            │
┌───────────────────────────┴───────────────────────────────┐
│                      Event Loop                            │
│              (проверяет: стек пуст?)                       │
└───────────────────────────┬───────────────────────────────┘
                            │
                            ▼
┌───────────────────────────────────────────────────────────┐
│                    Task Queue                              │
│              (очередь callback'ов)                         │
│                                                            │
│    [callback1] [callback2] [callback3]                    │
└───────────────────────────────────────────────────────────┘
                            ▲
                            │
┌───────────────────────────────────────────────────────────┐
│                      Web APIs                              │
│           (setTimeout, fetch, DOM events)                  │
└───────────────────────────────────────────────────────────┘
```

**Как это работает:**
1. Синхронный код выполняется в Call Stack
2. Асинхронные операции (setTimeout) передаются в Web APIs
3. Когда операция завершается, callback попадает в Task Queue
4. Event Loop проверяет: если Call Stack пуст — берёт callback из очереди

### setTimeout

`setTimeout` — функция, которая выполняет код через указанное время (в миллисекундах).

```javascript
setTimeout(callback, delay);
```

```javascript
console.log('Начало');

setTimeout(() => {
  console.log('Это выполнится через 1 секунду');
}, 1000);

console.log('Конец');

// Вывод:
// Начало
// Конец
// Это выполнится через 1 секунду
```

### Важно: setTimeout с 0 мс

Даже `setTimeout(..., 0)` не выполняется мгновенно — callback всё равно попадает в очередь и ждёт, пока освободится Call Stack.

```javascript
console.log('1');

setTimeout(() => {
  console.log('2');
}, 0);

console.log('3');

// Вывод:
// 1
// 3
// 2  ← выполнится после синхронного кода!
```

### clearTimeout

Можно отменить setTimeout до его выполнения:

```javascript
const timerId = setTimeout(() => {
  console.log('Это не выполнится');
}, 1000);

clearTimeout(timerId); // Отменяем таймер
```

---

## 💡 Примеры

### Пример 1: Порядок выполнения

```javascript
console.log('A');

setTimeout(() => {
  console.log('B');
}, 0);

console.log('C');

// Вывод: A, C, B
// Почему? setTimeout всегда асинхронный, даже с задержкой 0
```

### Пример 2: Вложенные setTimeout

```javascript
console.log('Старт');

setTimeout(() => {
  console.log('Таймер 1');

  setTimeout(() => {
    console.log('Таймер 2');
  }, 500);

}, 1000);

console.log('Конец');

// Вывод:
// Старт
// Конец
// (через 1 сек) Таймер 1
// (через 1.5 сек) Таймер 2
```

### Пример 3: Последовательные таймеры

```javascript
setTimeout(() => console.log('Первый'), 1000);
setTimeout(() => console.log('Второй'), 500);
setTimeout(() => console.log('Третий'), 1500);

// Вывод (по времени):
// (0.5 сек) Второй
// (1 сек) Первый
// (1.5 сек) Третий
```

### Пример 4: Использование в реальной задаче

```javascript
function showNotification(message) {
  const div = document.createElement('div');
  div.textContent = message;
  div.className = 'notification';
  document.body.appendChild(div);

  // Удаляем уведомление через 3 секунды
  setTimeout(() => {
    div.remove();
  }, 3000);
}

showNotification('Сохранено!');
```

---

## ✏️ Задание

### Описание

Напишите функцию `executeInOrder`, которая выполняет три callback-функции последовательно с задержками.

### Требования

1. Функция принимает три callback-функции: `first`, `second`, `third`
2. `first` выполняется сразу (через 0 мс от старта)
3. `second` выполняется через 100 мс от старта (т.е. `setTimeout(second, 100)`)
4. `third` выполняется через 200 мс от старта (т.е. `setTimeout(third, 200)`)
5. Функция возвращает объект с методом `cancel()` для отмены всех таймеров

Все задержки — **абсолютные от момента вызова**, а не относительные друг от друга. Три таймера запускаются одновременно с задержками 0, 100, 200 мс.

### Примеры использования

```javascript
const task = executeInOrder(
  () => console.log('Первый'),
  () => console.log('Второй'),
  () => console.log('Третий')
);

// Вывод (с задержками от момента вызова):
// (0мс)   Первый
// (100мс) Второй
// (200мс) Третий

// Или можно отменить:
task.cancel(); // Отменяет невыполненные таймеры
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Как запустить несколько таймеров одновременно, но с разными задержками? Что возвращает `setTimeout` и как это использовать для отмены? Подумайте, где хранить идентификаторы таймеров, чтобы метод `cancel()` мог к ним обратиться.

</details>

<details>
<summary>Подсказка 2: Структура</summary>

Все три таймера запускаются одновременно (не вложенно), каждый со своей абсолютной задержкой. Вам нужно сохранить идентификаторы таймеров, чтобы потом иметь возможность их отменить. Функция возвращает объект с методом, который умеет останавливать все запущенные таймеры.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

Создайте три вызова `setTimeout` — для каждого callback с соответствующей задержкой (0, 100, 200 мс). Каждый `setTimeout` возвращает числовой идентификатор — соберите их в массив. Верните объект с методом `cancel()`, который перебирает массив идентификаторов и отменяет каждый таймер с помощью `clearTimeout`.

</details>

---

## 🧪 Как проверить решение

```bash
npx vitest module-14/index.spec.js
```

Или в UI:
```bash
npm run test:ui
```

---

## 📖 Дополнительные материалы

- [MDN: Event Loop](https://developer.mozilla.org/ru/docs/Web/JavaScript/EventLoop)
- [JavaScript.info: Event Loop](https://learn.javascript.ru/event-loop)
- [Видео: What the heck is the event loop anyway?](https://www.youtube.com/watch?v=8aGhZQkoFbQ)

---

## ❓ Частые вопросы

**Q: Почему setTimeout(fn, 0) не выполняется сразу?**

A: Потому что callback всё равно попадает в очередь (Task Queue) и ждёт, пока Call Stack освободится. Минимальная задержка в браузерах — около 4мс.

**Q: Что такое "блокирующий код"?**

A: Код, который занимает Call Stack надолго (например, тяжёлые вычисления). Пока он выполняется, callback'и из очереди не могут запуститься.

**Q: Чем setTimeout отличается от setInterval?**

A: `setTimeout` выполняет код один раз через указанное время. `setInterval` выполняет код многократно с указанным интервалом.

**Q: Можно ли гарантировать точное время выполнения setTimeout?**

A: Нет. setTimeout гарантирует минимальную задержку, но не точную. Если Call Stack занят — callback выполнится позже.

---

## 🎓 Что дальше?

После выполнения переходите к **Модуль 15: Promises** — узнаете более удобный способ работы с асинхронностью.
