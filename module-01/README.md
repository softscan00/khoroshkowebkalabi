# Модуль 1: Переменные и типы данных

## 🎯 Цель модуля

Научиться создавать переменные, понимать разницу между `let` и `const`, познакомиться с основными типами данных в JavaScript: числа, строки, булевы значения, undefined и null.

---

## 📚 Теория

### Что такое переменная?

**Переменная** — это именованный контейнер для хранения данных. Представьте коробку с наклейкой (имя переменной), в которую вы можете положить что-то (значение).

```javascript
let age = 25; // Создали переменную age со значением 25
```

### Зачем это нужно?

Переменные позволяют:
- Хранить данные для дальнейшего использования
- Давать данным понятные имена (`userName` вместо просто `"Иван"`)
- Изменять данные в процессе работы программы

В реальной разработке переменные используются везде: для хранения данных пользователя, результатов вычислений, настроек приложения и т.д.

### Как это работает?

#### 1. Объявление переменных: `let` и `const`

**`let`** — для переменных, значение которых может измениться:
```javascript
let score = 0;
score = 10; // Можем изменить
score = 20; // И ещё раз изменить
```

**`const`** — для констант, значение которых НЕ изменится:
```javascript
const PI = 3.14;
// PI = 3.15; // Ошибка! Константу нельзя изменить
```

**Правило**: Используйте `const` по умолчанию, `let` — только когда знаете, что значение будет меняться.

#### 2. Типы данных

JavaScript — язык с динамической типизацией. Это значит, что переменная может хранить разные типы данных:

**Number (Числа)**
```javascript
const age = 25;           // Целое число
const price = 99.99;      // Дробное число
const negative = -10;     // Отрицательное число
```

**String (Строки)**
```javascript
const name = "Иван";              // Двойные кавычки
const city = 'Москва';            // Одинарные кавычки
const greeting = `Привет, ${name}`; // Шаблонная строка (backticks)
```

**Boolean (Булевы значения)**
```javascript
const isStudent = true;
const hasAccess = false;
```

**undefined** — переменная объявлена, но значение не присвоено:
```javascript
let something;
console.log(something); // undefined
```

**null** — явное отсутствие значения:
```javascript
const emptyValue = null;
```

#### 3. Функции — коротко

В задании вам нужно будет создать **функцию**. Функция — это именованный блок кода, который можно вызывать многократно:

```javascript
function sayHello(name) {   // объявление функции с параметром name
    return `Привет, ${name}!`;  // return возвращает результат
}

const result = sayHello("Иван"); // вызов функции
console.log(result); // "Привет, Иван!"
```

- **`function`** — ключевое слово для создания функции
- **`name`** — параметр (входные данные, которые функция получает при вызове)
- **`return`** — возвращает результат из функции

Подробнее функции разбираются в Модуле 5. Сейчас достаточно понимать эту базовую структуру.

---

## 💡 Примеры

### Пример 1: Работа с числами

```javascript
const a = 10;
const b = 5;
const sum = a + b;
const product = a * b;

console.log(sum);     // Вывод: 15
console.log(product); // Вывод: 50
```

**Объяснение**:
- Создали две константы с числами
- Сложили их и сохранили результат в `sum`
- Умножили и сохранили в `product`

### Пример 2: Работа со строками

```javascript
const firstName = "Иван";
const lastName = "Петров";
const fullName = firstName + " " + lastName; // Конкатенация

console.log(fullName); // Вывод: Иван Петров
```

**Объяснение**: Объединили (конкатенировали) две строки с помощью оператора `+`

### Пример 3: Шаблонные строки

```javascript
const name = "Мария";
const age = 22;
const message = `Меня зовут ${name}, мне ${age} года`;

console.log(message); // Вывод: Меня зовут Мария, мне 22 года
```

**Объяснение**: Использовали шаблонную строку (backticks `` ` ``) для вставки переменных через `${}`

### Пример 4: Проверка типа

```javascript
const num = 42;
const text = "Привет";

console.log(typeof num);  // Вывод: "number"
console.log(typeof text); // Вывод: "string"
```

**Объяснение**: Оператор `typeof` позволяет узнать тип данных переменной

---

## ✏️ Задание

### Описание

Создайте функцию `createGreeting`, которая принимает два параметра: имя (строка) и возраст (число), и возвращает строку-приветствие.

### Требования

- [ ] Функция должна называться `createGreeting`
- [ ] Функция принимает 2 параметра: `name` (строка), `age` (число)
- [ ] Функция возвращает строку вида: `"Привет, {имя}! Тебе {возраст} лет."`
- [ ] Используйте шаблонные строки (backticks) для создания результата

### Примеры использования

```javascript
createGreeting("Иван", 20);
// Возвращает: "Привет, Иван! Тебе 20 лет."

createGreeting("Мария", 25);
// Возвращает: "Привет, Мария! Тебе 25 лет."

createGreeting("Петр", 18);
// Возвращает: "Привет, Петр! Тебе 18 лет."
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Какой вид строк в JavaScript позволяет вставлять значения переменных прямо внутрь текста? Вспомните, какие кавычки для этого используются и какой синтаксис позволяет подставить переменную.

</details>

<details>
<summary>Подсказка 2: Структура</summary>

Функция должна вернуть одну строку. Используйте шаблонные строки (backticks `` ` ``), чтобы вставить параметры `name` и `age` прямо в текст через синтаксис `${выражение}`.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

Создайте шаблонную строку, которая начинается с `Привет, `, затем подставляет имя, потом `! Тебе `, затем возраст, и заканчивается на ` лет.`. Верните эту строку из функции через `return`. Обратите внимание на знаки препинания и пробелы — они должны точно совпадать с ожидаемым результатом.

</details>

---

## 🧪 Как проверить решение

1. Запустите тесты для этого модуля:
   ```bash
   npx vitest module-01/index.spec.js
   ```

2. Или запустите UI для визуальной проверки:
   ```bash
   npm run test:ui
   ```

3. Все тесты должны быть зелеными ✅

---

## 📖 Дополнительные материалы

- [MDN: Переменные](https://developer.mozilla.org/ru/docs/Learn/JavaScript/First_steps/Variables)
- [MDN: Типы данных](https://developer.mozilla.org/ru/docs/Web/JavaScript/Data_structures)
- [Learn.javascript.ru: Переменные](https://learn.javascript.ru/variables)
- [Learn.javascript.ru: Типы данных](https://learn.javascript.ru/types)

---

## ❓ Частые вопросы

**Q: В чем разница между `let` и `const`?**
A: `let` позволяет изменять значение переменной, `const` — нет. Используйте `const` по умолчанию для защиты от случайных изменений.

**Q: Когда использовать `var`?**
A: Не используйте `var` в современном JavaScript. Используйте `let` и `const`.

**Q: Можно ли изменить объект, объявленный через `const`?**
A: Нельзя переприсвоить переменную, но можно изменить свойства объекта:
```javascript
const user = { name: "Иван" };
user.name = "Петр"; // ✅ Можно
user = { name: "Петр" }; // ❌ Нельзя
```

**Q: Что такое `undefined` и `null`? В чем разница?**
A: `undefined` — значение по умолчанию для необъявленных переменных. `null` — явное указание "здесь ничего нет".

---

## 🎓 Что дальше?

После выполнения этого модуля переходите к **Модуль 2: Условия и операторы сравнения**, где вы научитесь принимать решения в коде с помощью `if`, `else` и операторов сравнения.
