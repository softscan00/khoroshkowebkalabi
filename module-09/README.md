# Модуль 9: Замыкания, методы объектов и spread оператор

## 🎯 Цель модуля

После прохождения модуля вы поймёте, что такое замыкания (closures), научитесь добавлять методы к объектам, копировать и объединять объекты с помощью spread оператора.

---

## 📚 Теория

### Методы объектов

**Метод** — это функция, которая является свойством объекта.

```javascript
const calculator = {
  value: 0,

  add: function(n) {
    this.value += n;
  },

  // Сокращённая запись (ES6)
  subtract(n) {
    this.value -= n;
  }
};

calculator.add(5);      // calculator.value = 5
calculator.subtract(2); // calculator.value = 3
```

### Ключевое слово `this`

Внутри метода `this` ссылается на сам объект:

```javascript
const user = {
  name: 'Иван',
  greet() {
    return `Привет, я ${this.name}`;
  }
};

user.greet(); // "Привет, я Иван"
```

### Spread оператор (`...`)

Spread оператор "разворачивает" объект или массив:

#### Копирование объекта

```javascript
const original = { a: 1, b: 2 };
const copy = { ...original };

console.log(copy); // { a: 1, b: 2 }
console.log(copy === original); // false (это разные объекты)
```

#### Объединение объектов

```javascript
const defaults = { theme: 'light', lang: 'ru' };
const userSettings = { theme: 'dark' };

const settings = { ...defaults, ...userSettings };
// { theme: 'dark', lang: 'ru' }
// Свойства из userSettings перезаписывают defaults
```

#### Добавление свойств при копировании

```javascript
const user = { name: 'Иван', age: 25 };
const updatedUser = { ...user, age: 26, city: 'Москва' };
// { name: 'Иван', age: 26, city: 'Москва' }
```

### Важно: spread создаёт поверхностную копию

```javascript
const original = {
  name: 'Иван',
  address: { city: 'Москва' }
};

const copy = { ...original };
copy.address.city = 'Питер';

console.log(original.address.city); // 'Питер' — изменился и оригинал!
```

Вложенные объекты копируются по ссылке, а не по значению.

### Замыкания (Closures)

**Замыкание** — одна из важнейших концепций JavaScript. Когда функция создаётся внутри другой функции, она «запоминает» переменные из внешней, даже после того как внешняя функция завершилась.

#### Простейший пример

```javascript
function createGreeter(greeting) {
    // greeting «замыкается» — сохраняется для внутренней функции
    return function(name) {
        return `${greeting}, ${name}!`;
    };
}

const hello = createGreeter('Привет');
const bye = createGreeter('Пока');

hello('Иван');  // "Привет, Иван!"
bye('Иван');    // "Пока, Иван!"
```

`createGreeter` уже завершилась, но возвращённая функция помнит значение `greeting`.

#### Зачем это нужно?

1. **Фабрика функций** — создание настроенных функций:
```javascript
function createMultiplier(factor) {
    return function(n) { return n * factor; };
}
const double = createMultiplier(2);
double(5); // 10
```

2. **Приватные данные** — скрытие переменных от внешнего доступа:
```javascript
function createCounter() {
    let count = 0;  // Эта переменная скрыта — снаружи к ней не добраться
    return {
        increment() { count++; },
        getValue() { return count; }
    };
}
```

Внешний код не имеет прямого доступа к `count` — она доступна только через методы объекта. В задании `createCounter` использует замыкание для хранения начального значения, чтобы метод `reset()` мог к нему обратиться.

---

## 💡 Примеры

### Пример 1: Объединение настроек

```javascript
function applySettings(userSettings) {
  const defaults = {
    theme: 'light',
    fontSize: 14,
    notifications: true
  };

  return { ...defaults, ...userSettings };
}

applySettings({ theme: 'dark' });
// { theme: 'dark', fontSize: 14, notifications: true }

applySettings({ fontSize: 18, notifications: false });
// { theme: 'light', fontSize: 18, notifications: false }
```

### Пример 2: Обновление объекта

```javascript
function updateUser(user, updates) {
  return { ...user, ...updates };
}

const user = { name: 'Иван', age: 25, city: 'Москва' };
const updated = updateUser(user, { age: 26 });
// { name: 'Иван', age: 26, city: 'Москва' }
```

### Пример 3: Объект с методом

```javascript
const counter = {
  count: 0,

  increment() {
    this.count++;
    return this.count;
  },

  reset() {
    this.count = 0;
  }
};

counter.increment(); // 1
counter.increment(); // 2
counter.reset();     // count = 0
```

---

## ✏️ Задание

### Описание

Создайте три функции:

1. **`createMultiplier(factor)`** — фабрика функций с замыканием
2. **`mergeObjects(obj1, obj2)`** — объединяет два объекта с помощью spread оператора
3. **`createCounter(initial)`** — создаёт объект-счётчик с методами

### Требования

**createMultiplier:**
- [ ] Принимает число `factor`
- [ ] Возвращает **функцию**, которая принимает число и умножает его на `factor`
- [ ] Каждый вызов `createMultiplier` создаёт независимую функцию

**mergeObjects:**
- [ ] Принимает два объекта
- [ ] Возвращает новый объект с объединёнными свойствами
- [ ] Свойства из `obj2` перезаписывают свойства из `obj1`
- [ ] Исходные объекты не изменяются

**createCounter:**
- [ ] Принимает начальное значение (по умолчанию 0)
- [ ] Возвращает объект со свойством `value` и методами:
  - `increment()` — увеличивает value на 1, возвращает новое значение
  - `decrement()` — уменьшает value на 1, возвращает новое значение
  - `reset()` — сбрасывает value в начальное значение

### Примеры использования

```javascript
// createMultiplier
const double = createMultiplier(2);
double(5);   // 10
double(3);   // 6

const triple = createMultiplier(3);
triple(4);   // 12

// mergeObjects
mergeObjects({ a: 1 }, { b: 2 });         // { a: 1, b: 2 }
mergeObjects({ a: 1 }, { a: 5 });         // { a: 5 }
mergeObjects({ x: 1, y: 2 }, { y: 3, z: 4 }); // { x: 1, y: 3, z: 4 }

// createCounter
const counter = createCounter(10);
counter.value;       // 10
counter.increment(); // 11
counter.increment(); // 12
counter.decrement(); // 11
counter.reset();     // value снова 10
counter.value;       // 10
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Для `createMultiplier`: функция может возвращать другую функцию. Внутренняя функция «помнит» переменные из внешней — это замыкание.

Для `mergeObjects`: какой оператор позволяет «развернуть» свойства одного объекта внутри другого? Что произойдёт, если оба объекта содержат одинаковый ключ?

Для `createCounter`: как сохранить начальное значение так, чтобы `reset()` мог к нему обратиться позже?

</details>

<details>
<summary>Подсказка 2: Структура</summary>

`createMultiplier` — внешняя функция принимает `factor`, а возвращает внутреннюю функцию, которая принимает число. Внутренняя функция использует `factor` из замыкания для вычисления результата.

`mergeObjects` — spread оператор (`...`) позволяет «развернуть» свойства объекта внутри нового объекта. Порядок важен: последние свойства перезаписывают предыдущие.

`createCounter` — сохраните `initial` в отдельную переменную. Верните объект с `value` и методами, которые изменяют `this.value`.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

`createMultiplier`: верните анонимную функцию (или стрелочную), которая умножает свой аргумент на `factor`. Параметр `factor` остаётся доступен через замыкание.

`mergeObjects`: создайте новый объект, развернув в нём оба исходных объекта через spread. Второй объект должен идти после первого, чтобы его свойства имели приоритет.

`createCounter`: сохраните `initial` в переменную (например `startValue`) до `return`. Методы `increment`/`decrement` меняют `this.value` на 1. Метод `reset` присваивает `this.value` значение из замыкания.

</details>

---

## 🧪 Как проверить решение

1. Запустите тесты для этого модуля:
   ```bash
   npx vitest module-09/index.spec.js
   ```

2. Или запустите UI для визуальной проверки:
   ```bash
   npm run test:ui
   ```

---

## 📖 Дополнительные материалы

- [MDN: Spread syntax](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Operators/Spread_syntax)
- [MDN: this](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Operators/this)
- [JavaScript.info: Методы объекта, this](https://learn.javascript.ru/object-methods)
- [JavaScript.info: Spread оператор](https://learn.javascript.ru/rest-parameters-spread#spread-operator)

---

## ❓ Частые вопросы

**Q: Чем spread отличается от Object.assign()?**
A: Результат одинаковый, но spread короче и читаемее: `{ ...a, ...b }` vs `Object.assign({}, a, b)`.

**Q: Почему `this` не работает в стрелочных функциях?**
A: Стрелочные функции не имеют своего `this`. Для методов объектов используйте обычные функции.

**Q: Как сделать глубокую копию объекта?**
A: Для простых случаев: `JSON.parse(JSON.stringify(obj))`. Для сложных — библиотеки типа lodash.

**Q: Можно ли использовать spread с массивами?**
A: Да! `[...arr1, ...arr2]` объединяет массивы, `[...arr]` копирует массив.

---

## 🎓 Что дальше?

После выполнения этого модуля переходите к **Модуль 10: Set и Map**, где вы узнаете о структурах данных Set и Map.
