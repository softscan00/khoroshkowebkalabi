# Модуль 8: Debugging (Отладка)

## 🎯 Цель модуля

Научиться находить и исправлять ошибки в коде с помощью инструментов отладки.

---

## 📚 Теория

### Что такое debugging?

**Debugging** (отладка) — процесс поиска и исправления ошибок в коде. Название происходит от слова "bug" (жук) — так называли первые ошибки в компьютерах.

### Виды ошибок

1. **Синтаксические** — код не запускается
   ```javascript
   console.log('Hello'  // Забыли закрыть скобку
   ```

2. **Ошибки времени выполнения** — код падает при работе
   ```javascript
   const user = null;
   console.log(user.name);  // TypeError: Cannot read property 'name' of null
   ```

3. **Логические** — код работает, но неправильно
   ```javascript
   function isAdult(age) {
     return age > 18;  // Баг: должно быть >= 18
   }
   ```

### console — ваш первый инструмент

```javascript
// Простой вывод
console.log('Значение:', variable);

// Вывод объекта в виде таблицы
console.table([{name: 'Иван', age: 25}, {name: 'Мария', age: 30}]);

// Группировка логов
console.group('Загрузка данных');
console.log('Шаг 1: запрос');
console.log('Шаг 2: обработка');
console.groupEnd();

// Замер времени
console.time('операция');
// ... код ...
console.timeEnd('операция');  // операция: 150ms

// Условный вывод
console.assert(age >= 0, 'Возраст не может быть отрицательным');

// Предупреждение и ошибка
console.warn('Устаревший метод');
console.error('Критическая ошибка!');
```

### debugger — точка останова в коде

```javascript
function calculateTotal(items) {
  let total = 0;

  debugger;  // Код остановится здесь

  for (const item of items) {
    total += item.price;
  }

  return total;
}
```

Когда браузер (с открытыми DevTools) или Node.js (с флагом `--inspect`) встречает `debugger`, выполнение останавливается и можно исследовать состояние программы.

### DevTools браузера (F12)

1. **Console** — вывод логов, выполнение кода
2. **Sources** — просмотр кода, точки останова
3. **Network** — сетевые запросы
4. **Elements** — DOM-дерево

#### Точки останова (Breakpoints)

В вкладке Sources:
- Кликните на номер строки — обычная точка останова
- ПКМ → "Add conditional breakpoint" — остановка по условию
- ПКМ → "Add logpoint" — вывод без остановки

#### Навигация при отладке

| Кнопка | Действие |
|--------|----------|
| ▶️ Resume | Продолжить выполнение |
| ⤵️ Step over | Выполнить строку (не входя в функции) |
| ⬇️ Step into | Войти в функцию |
| ⬆️ Step out | Выйти из функции |

### Отладка в Node.js

```bash
# Запуск с инспектором
node --inspect index.js

# Или с остановкой на первой строке
node --inspect-brk index.js
```

Затем откройте `chrome://inspect` в Chrome.

### Отладка в VS Code

1. Откройте файл
2. Поставьте точку останова (клик слева от номера строки)
3. Нажмите F5 или "Run and Debug"
4. Выберите "Node.js"

---

## 💡 Примеры

### Пример 1: Поиск бага с console.log

```javascript
function findUser(users, id) {
  console.log('Ищем пользователя с id:', id);
  console.log('Всего пользователей:', users.length);

  for (let i = 0; i <= users.length; i++) {  // Баг: <= вместо <
    console.log('Проверяем индекс:', i, 'пользователь:', users[i]);
    if (users[i].id === id) {
      return users[i];
    }
  }
}
// Лог покажет, что на последней итерации users[i] = undefined
```

### Пример 2: Отладка асинхронного кода

```javascript
async function loadData() {
  console.log('1. Начало загрузки');

  try {
    const response = await fetch('/api/data');
    console.log('2. Ответ получен:', response.status);

    const data = await response.json();
    console.log('3. Данные:', data);

    return data;
  } catch (error) {
    console.error('Ошибка:', error);
  }
}
```

### Пример 3: Проверка типов

```javascript
function add(a, b) {
  console.log('typeof a:', typeof a);  // Может быть string!
  console.log('typeof b:', typeof b);

  return a + b;  // '5' + 3 = '53', а не 8
}
```

---

## ✏️ Задание

### Описание

В файле `index.js` есть три функции с багами: `sumPositive`, `getFullName` и `filterAdults`. Найдите и исправьте ошибки в каждой из них, используя инструменты отладки.

### Требования

1. Функция `sumPositive(numbers)` — должна суммировать только положительные числа
2. Функция `getFullName(user)` — должна возвращать "Имя Фамилия"
3. Функция `filterAdults(users)` — должна возвращать пользователей 18+ лет

### Как отлаживать

1. Запустите тесты: `npx vitest module-08/index.spec.js`
2. Посмотрите какие тесты падают
3. Добавьте `console.log` или `debugger` в функции
4. Найдите и исправьте баги

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

В каждой функции есть одна небольшая логическая ошибка. Подумайте: чем «положительное число» отличается от «неотрицательного»? В каком порядке обычно пишут «Имя Фамилия»? Какой оператор включает граничное значение?

</details>

<details>
<summary>Подсказка 2: Структура</summary>

В каждой функции ровно одна ошибка. Попробуйте добавить `console.log` перед ключевыми операциями и сравнить фактическое поведение с ожидаемым. Обратите внимание на граничные значения (ноль, возраст ровно 18) и на порядок данных в выводе.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

В `sumPositive` подумайте, является ли ноль положительным числом, и проверьте, какой оператор сравнения используется — строгий (`>`) или нестрогий (`>=`). В `getFullName` обратите внимание на порядок, в котором подставляются `firstName` и `lastName` в результирующую строку — он может быть перепутан. В `filterAdults` проверьте граничное условие: человек, которому ровно 18, считается взрослым — убедитесь, что оператор сравнения это учитывает.

</details>

---

## 🧪 Как проверить решение

```bash
npx vitest module-08/index.spec.js
```

---

## 📖 Дополнительные материалы

- [Chrome DevTools — документация](https://developer.chrome.com/docs/devtools/)
- [Отладка в VS Code](https://code.visualstudio.com/docs/editor/debugging)
- [Node.js Debugging Guide](https://nodejs.org/en/docs/guides/debugging-getting-started/)

---

## ❓ Частые вопросы

**Q: Когда использовать console.log, а когда debugger?**

A: `console.log` — для быстрой проверки значений. `debugger` — когда нужно пошагово пройти код и посмотреть состояние на каждом шаге.

**Q: Как отлаживать код, который работает только в браузере?**

A: Откройте DevTools (F12), перейдите в Sources, найдите файл и поставьте breakpoint.

**Q: Можно ли оставлять console.log в продакшен-коде?**

A: Нет, это считается плохой практикой. Используйте линтер (ESLint) с правилом `no-console`.

---

## 🎓 Что дальше?

После выполнения переходите к **Модуль 9: Объекты — методы и spread** — научитесь добавлять методы к объектам и использовать spread оператор.
