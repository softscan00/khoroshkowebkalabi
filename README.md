# JavaScript: Практический курс

Курс из 32 модулей для изучения JavaScript с нуля. Каждый модуль -- самостоятельное задание с теорией, тестами и подсказками. Вы пишете код, запускаете тесты -- и сразу видите, правильно ли решение.

## Содержание

- [Быстрый старт](#быстрый-старт)
- [Как устроен курс](#как-устроен-курс)
- [Программа курса](#программа-курса)
- [Как работать с заданием](#как-работать-с-заданием)
- [Тестирование с Vitest](#тестирование-с-vitest)
- [Структура проекта](#структура-проекта)
- [Группы и чаты](#группы-и-чаты)

---

## Быстрый старт

1. Проверьте [Node.js](https://nodejs.org/) (версия 18 или выше)

```bash
node -v
```
2. Установите зависимости:

```bash
npm install
```

3. Запустите тесты в браузере:

```bash
npm run test
```

4. Откройте первый модуль: `module-01/README.md`

---

## Как устроен курс

Каждый модуль -- отдельная папка с файлами:

| Файл | Назначение |
|------|------------|
| `README.md` | Теория и описание задания |
| `index.js` | Ваш файл -- пишите решение здесь |
| `index.spec.js` | Тесты (не изменяйте) |

Порядок работы: прочитайте теорию в README, напишите код в `index.js`, запустите тесты. Все тесты зелёные -- модуль пройден.

---

## Программа курса

### Блок 1: Основы JavaScript (модули 01-08)

| # | Тема | Что изучаете |
|---|------|-------------|
| 01 | Переменные и типы данных | `let`, `const`, типы, шаблонные строки |
| 02 | Условия | `if/else`, операторы сравнения, логические операторы |
| 03 | Массивы | Создание, индексы, `length`, доступ к элементам |
| 04 | Циклы | `for`, `while`, перебор массивов |
| 05 | Функции | Declaration, Expression, Arrow functions |
| 06 | Методы массивов | `push`, `pop`, `shift`, `unshift` |
| 07 | Объекты | Создание, свойства, методы |
| 08 | Отладка | Поиск и исправление багов, DevTools |

### Блок 2: Продвинутый JavaScript (модули 09-12)

| # | Тема | Что изучаете |
|---|------|-------------|
| 09 | Замыкания и spread | Замыкания, методы объектов, `...spread` |
| 10 | Set и Map | Коллекции, хэш-функции, уникальные значения |
| 11 | Git | Система контроля версий, основные команды |
| 12 | npm | Менеджер пакетов, `package.json`, установка библиотек |

### Блок 3: Асинхронность (модули 13-16)

| # | Тема | Что изучаете |
|---|------|-------------|
| 13 | Callbacks | Функции обратного вызова, error-first паттерн |
| 14 | Event Loop | Однопоточность JS, `setTimeout`, очередь задач |
| 15 | Promises | `Promise`, `resolve`/`reject`, цепочки `.then` |
| 16 | async/await | Асинхронные функции, последовательное выполнение |

### Блок 4: Браузер и DOM (модули 17-20)

| # | Тема | Что изучаете |
|---|------|-------------|
| 17 | HTML основы | Структура HTML, генерация разметки |
| 18 | DOM | `querySelector`, `textContent`, `classList` |
| 19 | События | `addEventListener`, `click`, `input`, `submit` |
| 20 | Формы и валидация | `FormData`, проверка данных, объект ошибок |

### Блок 5: Сетевые запросы (модули 21-22)

| # | Тема | Что изучаете |
|---|------|-------------|
| 21 | Fetch | GET/POST запросы, `fetch`, `response.json()` |
| 22 | Fetch: ошибки | `response.ok`, `Promise.all`, `Promise.allSettled` |

### Блок 6: Серверная разработка (модули 23-27)

| # | Тема | Что изучаете |
|---|------|-------------|
| 23 | HTTP-сервер | `http.createServer`, роутинг, JSON-ответы |
| 24 | Routing и URL | `URL API`, параметры пути, пагинация |
| 25 | POST и DELETE | Чтение тела запроса, CRUD-операции |
| 26 | CORS | Same-Origin Policy, CORS-заголовки, preflight |
| 27 | TODO-приложение | Fullstack: сервер + фронтенд, REST API |

### Блок 7: Фреймворки (модули 28-32)

| # | Тема | Что изучаете |
|---|------|-------------|
| 28 | Работа с файлами | Модуль `fs`, JSON-хранилище, персистентность |
| 29 | Express | Фреймворк Express, middleware, `req.params` |
| 30 | React: основы | Компоненты, props, `useState`, JSX |
| 31 | React: данные | `useEffect`, загрузка данных, формы |
| 32 | Финальный проект | Fullstack Notes App: React + Express + файловое хранилище |

---

## Как работать с заданием

1. Откройте `module-XX/README.md` и прочитайте теорию
2. Откройте `module-XX/index.js` -- там заготовка с комментариями
3. Напишите своё решение
4. Запустите тесты:

```bash
# Один модуль
npx vitest module-01/index.spec.js

# Один модуль в браузере
npx vitest module-01/index.spec.js --ui

# Все модули
npm run test
```

5. Если застряли -- в README каждого модуля есть 3 уровня подсказок (от общей идеи до почти-решения)

---

## Тестирование с Vitest

Проект использует [Vitest](https://vitest.dev/) для запуска тестов.

### Команды

| Команда | Описание |
|---------|----------|
| `npm run test` | Запуск тестов в браузере (рекомендуется) |

### Vitest UI

Запустив `npm run test`, вы получите веб-интерфейс с тремя вкладками:

- **Report** -- сводка по тестам: сколько прошло, сколько упало
- **Code** -- исходный код тестов и связь с проверяемыми функциями
- **Console** -- логи и сообщения, полезно для отладки через `console.log`

---

## Структура проекта

```
js-test/
├── module-01/          # Модули с заданиями
│   ├── README.md       #   Теория и задание
│   ├── index.js        #   Ваш код (редактируйте)
│   ├── index.spec.js   #   Тесты (не трогайте)
│   └── index.solution.js  # Эталонное решение
├── module-02/
│   └── ...
├── ...
├── module-32/
├── vitest.config.js    # Конфигурация тестов
├── vitest.setup.js     # Настройка тестового окружения
└── package.json        # Зависимости и скрипты
```