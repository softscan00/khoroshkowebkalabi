# Модуль 4: Циклы (for, while)

## 🎯 Цель модуля

После прохождения модуля вы научитесь использовать циклы для повторения действий: перебирать массивы, выполнять код определенное количество раз и автоматизировать повторяющиеся задачи.

---

## 📚 Теория

### Что такое циклы?

**Цикл** — это конструкция, которая позволяет выполнять один и тот же код несколько раз. Представьте, что вам нужно вывести числа от 1 до 100. Вместо того чтобы писать 100 строк кода, вы можете использовать цикл, который сделает это за вас.

Циклы — это как конвейер на фабрике: одно и то же действие повторяется для каждого элемента.

### Зачем это нужно?

В реальной разработке циклы используются постоянно:
- Обработка списка пользователей
- Вывод товаров в интернет-магазине
- Суммирование или фильтрация данных
- Поиск нужного элемента в массиве
- Отправка множественных запросов к API

Без циклов любая программа, работающая с наборами данных, была бы невозможна.

### Как это работает?

В JavaScript есть несколько видов циклов, но мы начнем с двух основных:

#### 1. Цикл `for`

Цикл `for` используется, когда вы **заранее знаете**, сколько раз нужно повторить действие.

**Синтаксис:**
```javascript
for (инициализация; условие; изменение) {
  // код, который будет повторяться
}
```

**Как это работает:**
1. **Инициализация** — выполняется один раз перед началом цикла (обычно создаем счетчик)
2. **Условие** — проверяется перед каждой итерацией (если `true` — цикл продолжается)
3. **Изменение** — выполняется после каждой итерации (обычно увеличиваем счетчик)

#### 2. Цикл `while`

Цикл `while` используется, когда вы **не знаете заранее**, сколько раз нужно повторить действие, но знаете **условие**, при котором нужно продолжать.

**Синтаксис:**
```javascript
while (условие) {
  // код, который будет повторяться
}
```

**Как это работает:**
- Перед каждой итерацией проверяется условие
- Если условие `true` — код выполняется
- Если условие `false` — цикл завершается

⚠️ **Важно:** Если условие никогда не станет `false`, цикл станет **бесконечным** и зависнет!

---

## 💡 Примеры

### Пример 1: Базовый цикл `for`

```javascript
// Выводим числа от 1 до 5
for (let i = 1; i <= 5; i++) {
  console.log(i);
}
// Вывод: 1, 2, 3, 4, 5
```

**Объяснение:**
1. `let i = 1` — создаем переменную-счетчик со значением 1
2. `i <= 5` — пока i меньше или равно 5, цикл продолжается
3. `i++` — после каждой итерации увеличиваем i на 1
4. `console.log(i)` — выводим текущее значение i

### Пример 2: Перебор массива с `for`

```javascript
const numbers = [10, 20, 30, 40, 50];
let sum = 0;

for (let i = 0; i < numbers.length; i++) {
  sum += numbers[i]; // Прибавляем каждый элемент к sum
}

console.log(sum); // Вывод: 150
```

**Объяснение:**
1. `i = 0` — массивы начинаются с индекса 0
2. `i < numbers.length` — пока i меньше длины массива (5 элементов)
3. `numbers[i]` — получаем элемент по текущему индексу
4. `sum += numbers[i]` — добавляем элемент к общей сумме

### Пример 3: Цикл `while` с условием

```javascript
let count = 0;

while (count < 3) {
  console.log(`Счетчик: ${count}`);
  count++; // ВАЖНО: не забыть увеличить счетчик!
}
// Вывод:
// Счетчик: 0
// Счетчик: 1
// Счетчик: 2
```

**Объяснение:**
1. Устанавливаем начальное значение `count = 0`
2. Пока `count < 3`, выполняем код внутри цикла
3. `count++` увеличивает счетчик на 1 после каждой итерации
4. Когда `count` станет равен 3, условие станет `false` и цикл завершится

### Пример 4: Поиск элемента в массиве

```javascript
const fruits = ['яблоко', 'банан', 'апельсин', 'груша'];
const target = 'апельсин';
let found = false;

for (let i = 0; i < fruits.length; i++) {
  if (fruits[i] === target) {
    found = true;
    console.log(`Найдено на позиции ${i}`);
    break; // Прерываем цикл, так как нашли что искали
  }
}

if (!found) {
  console.log('Не найдено');
}
// Вывод: Найдено на позиции 2
```

**Объяснение:**
1. Перебираем массив фруктов
2. Сравниваем каждый элемент с искомым значением
3. Если нашли — выводим позицию и прерываем цикл оператором `break`
4. `break` немедленно завершает цикл (экономия ресурсов)

---

## ✏️ Задание

### Описание

Напишите функцию `sumArray`, которая принимает массив чисел и возвращает их сумму, используя цикл `for`.

### Требования

- [ ] Функция должна называться `sumArray`
- [ ] Функция принимает один параметр — массив чисел
- [ ] Функция использует цикл `for` для перебора массива
- [ ] Функция возвращает сумму всех чисел в массиве
- [ ] Если массив пустой, функция возвращает `0`

### Примеры использования

```javascript
sumArray([1, 2, 3, 4, 5]); // Вывод: 15
sumArray([10, 20, 30]); // Вывод: 60
sumArray([]); // Вывод: 0
sumArray([-5, 5, 10]); // Вывод: 10
sumArray([100]); // Вывод: 100
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Чтобы собрать сумму элементов, нужно «пройтись» по каждому элементу массива и накапливать результат. Какая конструкция JavaScript позволяет последовательно перебрать все элементы? С какого значения должен начинаться накопитель?

</details>

<details>
<summary>Подсказка 2: Структура</summary>

Вам понадобится переменная-накопитель (аккумулятор), которая начинается с нуля. Затем с помощью цикла `for` переберите все элементы массива и на каждом шаге прибавляйте текущий элемент к накопителю. В конце верните накопленное значение.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

Создайте переменную `sum` со значением `0`. Организуйте цикл `for`, где счётчик `i` идёт от `0` до `arr.length` (не включительно). На каждой итерации обращайтесь к элементу массива по индексу `i` и прибавляйте его к `sum`. После завершения цикла верните `sum`. Для пустого массива цикл просто не выполнится ни разу, и функция вернёт начальное значение — `0`.

</details>

---

## 🧪 Как проверить решение

1. Запустите тесты для этого модуля:
   ```bash
   npx vitest module-04/index.spec.js
   ```

2. Или запустите UI для визуальной проверки:
   ```bash
   npm run test:ui
   ```

---

## 📖 Дополнительные материалы

- [MDN: Циклы и итерация](https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Loops_and_iteration)
- [MDN: for statement](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/for)
- [MDN: while statement](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/while)
- [JavaScript.info: Циклы while и for](https://learn.javascript.ru/while-for)

---

## ❓ Частые вопросы

**Q: В чем разница между `for` и `while`?**
A: `for` удобен, когда вы знаете количество итераций заранее (например, длину массива). `while` используется, когда количество итераций зависит от какого-то условия (например, пока пользователь не введет правильный пароль).

**Q: Что делает оператор `break`?**
A: `break` немедленно завершает цикл, даже если условие еще истинно. Полезно, когда вы нашли нужный элемент и нет смысла продолжать поиск.

**Q: Что такое бесконечный цикл и как его избежать?**
A: Бесконечный цикл происходит, когда условие никогда не становится `false`. Например:
```javascript
while (true) {
  console.log('Это никогда не закончится!');
}
```
Чтобы избежать: всегда убедитесь, что условие цикла в какой-то момент станет `false`.

**Q: Почему `i < arr.length`, а не `i <= arr.length`?**
A: Массивы индексируются с 0. Если массив имеет 5 элементов, индексы: 0, 1, 2, 3, 4. `arr.length` равен 5, но `arr[5]` — это уже за пределами массива! Поэтому используем `i < arr.length`.

**Q: Можно ли использовать `let` вместо `var` в цикле `for`?**
A: Да, и даже нужно! `let` создает новую переменную для каждой итерации цикла, что безопаснее. `var` может привести к неожиданным проблемам в сложных случаях.

---

## 🎓 Что дальше?

После выполнения этого модуля переходите к **Модуль 5: Функции (declaration, expression, arrow)**, где вы изучите различные способы создания функций и научитесь писать более чистый и переиспользуемый код.
