# Модуль 6: Методы массивов (push, pop, shift, unshift)

## 🎯 Цель модуля

После прохождения модуля вы научитесь добавлять и удалять элементы массива с обоих концов, используя встроенные методы JavaScript.

---

## 📚 Теория

### Четыре основных метода

JavaScript предоставляет четыре метода для работы с началом и концом массива:

| Метод | Действие | Возвращает |
|-------|----------|------------|
| `push()` | Добавляет в конец | Новую длину массива |
| `pop()` | Удаляет с конца | Удалённый элемент |
| `unshift()` | Добавляет в начало | Новую длину массива |
| `shift()` | Удаляет с начала | Удалённый элемент |

### Визуализация

```
                    unshift ←  [a, b, c, d]  ← push
                    shift   →  [a, b, c, d]  → pop
```

### Подробнее о каждом методе

#### push() — добавить в конец

```javascript
const fruits = ['яблоко', 'банан'];
fruits.push('апельсин');
console.log(fruits); // ['яблоко', 'банан', 'апельсин']

// Можно добавить несколько элементов
fruits.push('груша', 'киви');
console.log(fruits); // ['яблоко', 'банан', 'апельсин', 'груша', 'киви']
```

#### pop() — удалить с конца

```javascript
const fruits = ['яблоко', 'банан', 'апельсин'];
const last = fruits.pop();
console.log(last);   // 'апельсин'
console.log(fruits); // ['яблоко', 'банан']
```

#### unshift() — добавить в начало

```javascript
const fruits = ['банан', 'апельсин'];
fruits.unshift('яблоко');
console.log(fruits); // ['яблоко', 'банан', 'апельсин']
```

#### shift() — удалить с начала

```javascript
const fruits = ['яблоко', 'банан', 'апельсин'];
const first = fruits.shift();
console.log(first);  // 'яблоко'
console.log(fruits); // ['банан', 'апельсин']
```

### Важно: методы изменяют оригинальный массив

Все четыре метода **мутируют** (изменяют) исходный массив:

```javascript
const arr = [1, 2, 3];
arr.push(4);      // arr теперь [1, 2, 3, 4]
arr.pop();        // arr теперь [1, 2, 3]
arr.unshift(0);   // arr теперь [0, 1, 2, 3]
arr.shift();      // arr теперь [1, 2, 3]
```

---

## 💡 Примеры

### Пример 1: Очередь (FIFO)

Очередь работает по принципу "первый пришёл — первый вышел":

```javascript
const queue = [];

// Добавляем в конец очереди
queue.push('Клиент 1');
queue.push('Клиент 2');
queue.push('Клиент 3');
// queue: ['Клиент 1', 'Клиент 2', 'Клиент 3']

// Обслуживаем первого в очереди
const served = queue.shift(); // 'Клиент 1'
// queue: ['Клиент 2', 'Клиент 3']
```

### Пример 2: Стек (LIFO)

Стек работает по принципу "последний пришёл — первый вышел":

```javascript
const stack = [];

// Кладём на стек
stack.push('Тарелка 1');
stack.push('Тарелка 2');
stack.push('Тарелка 3');
// stack: ['Тарелка 1', 'Тарелка 2', 'Тарелка 3']

// Берём верхнюю тарелку
const top = stack.pop(); // 'Тарелка 3'
// stack: ['Тарелка 1', 'Тарелка 2']
```

### Пример 3: Ротация массива

```javascript
// Сдвиг влево: первый элемент становится последним
function rotateLeft(arr) {
  const first = arr.shift();
  arr.push(first);
  return first;
}

const nums = [1, 2, 3, 4];
rotateLeft(nums); // nums: [2, 3, 4, 1]
```

---

## ✏️ Задание

### Описание

Создайте две функции для ротации массива:

1. **`rotateLeft(arr)`** — сдвигает массив влево (первый элемент перемещается в конец)
2. **`rotateRight(arr)`** — сдвигает массив вправо (последний элемент перемещается в начало)

Обе функции должны изменять оригинальный массив и возвращать перемещённый элемент.

### Требования

- [ ] `rotateLeft` удаляет первый элемент и добавляет его в конец
- [ ] `rotateRight` удаляет последний элемент и добавляет его в начало
- [ ] Обе функции возвращают перемещённый элемент
- [ ] Для пустого массива функции возвращают `undefined`

### Примеры использования

```javascript
// rotateLeft
const arr1 = [1, 2, 3, 4];
rotateLeft(arr1);  // возвращает 1
console.log(arr1); // [2, 3, 4, 1]

// rotateRight
const arr2 = [1, 2, 3, 4];
rotateRight(arr2); // возвращает 4
console.log(arr2); // [4, 1, 2, 3]

// Пустой массив
rotateLeft([]);  // undefined
rotateRight([]); // undefined
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Подумайте, какие два метода массива удаляют элемент и возвращают его? Один работает с началом массива, другой — с концом. А какие два метода добавляют элемент? Какую пару методов нужно скомбинировать для каждой функции?

</details>

<details>
<summary>Подсказка 2: Структура</summary>

Каждая функция выполняет два действия: сначала извлекает элемент с одного конца массива, затем вставляет его на противоположный конец. Подумайте, какая **пара** методов из четвёрки `push`/`pop`/`shift`/`unshift` нужна для каждой ротации. Не забудьте вернуть перемещённый элемент.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

Для `rotateLeft`: используйте метод, который удаляет элемент с начала массива, сохраните его в переменную, затем добавьте этот элемент в конец массива другим методом и верните его. Для `rotateRight`: используйте метод, который удаляет элемент с конца массива, сохраните его, затем добавьте в начало массива и верните. Для пустого массива методы удаления сами вернут `undefined`, а метод добавления с аргументом `undefined` не навредит — но подумайте, нужно ли вообще что-то добавлять в пустой массив.

</details>

---

## 🧪 Как проверить решение

1. Запустите тесты для этого модуля:
   ```bash
   npx vitest module-06/index.spec.js
   ```

2. Или запустите UI для визуальной проверки:
   ```bash
   npm run test:ui
   ```

---

## 📖 Дополнительные материалы

- [MDN: Array.push()](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array/push)
- [MDN: Array.pop()](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array/pop)
- [MDN: Array.shift()](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array/shift)
- [MDN: Array.unshift()](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array/unshift)
- [JavaScript.info: Методы массивов](https://learn.javascript.ru/array-methods)

---

## ❓ Частые вопросы

**Q: Почему push/pop быстрее чем unshift/shift?**
A: `push` и `pop` работают с концом массива — просто добавляют или удаляют элемент. А `unshift` и `shift` работают с началом — им приходится сдвигать все остальные элементы, что медленнее для больших массивов.

**Q: Что вернёт pop() или shift() для пустого массива?**
A: Вернут `undefined`, ошибки не будет.

**Q: Можно ли добавить несколько элементов сразу?**
A: Да! `push(1, 2, 3)` и `unshift(1, 2, 3)` добавят три элемента за один вызов.

**Q: Эти методы создают новый массив?**
A: Нет, они изменяют существующий массив (мутируют его). Если нужен новый массив, используйте spread-оператор: `[...arr, newItem]`.

---

## 🎓 Что дальше?

После выполнения этого модуля переходите к **Модуль 7: Объекты**, где вы научитесь создавать объекты и работать с их свойствами.
