# Модуль 3: Массивы — основы работы

## 🎯 Цель модуля

После прохождения модуля вы научитесь создавать массивы, получать доступ к элементам по индексу, работать со свойством `length` и добавлять элементы в массив.

---

## 📚 Теория

### Что такое массив?

**Массив** — это упорядоченная коллекция элементов. Представьте список покупок, список задач или список друзей — это и есть массивы в реальной жизни.

В JavaScript массив может содержать любые типы данных: числа, строки, boolean и даже другие массивы.

### Зачем это нужно?

В реальной разработке массивы используются повсеместно:
- Список товаров в корзине
- Список пользователей
- Результаты поиска
- История действий
- Любые данные, которых больше одного

Без массивов пришлось бы создавать отдельную переменную для каждого элемента, что невозможно при работе с большими объемами данных.

### Как это работает?

#### Создание массива

```javascript
const fruits = ['яблоко', 'банан', 'апельсин'];
const numbers = [1, 2, 3, 4, 5];
const empty = [];
```

#### Индексы массива

Элементы массива пронумерованы, начиная с **нуля**:

```
['яблоко', 'банан', 'апельсин']
    0         1          2
```

#### Доступ к элементам

Чтобы получить элемент, используем квадратные скобки с индексом:

```javascript
const fruits = ['яблоко', 'банан', 'апельсин'];

fruits[0]; // 'яблоко'
fruits[1]; // 'банан'
fruits[2]; // 'апельсин'
```

#### Свойство length

`length` возвращает количество элементов в массиве:

```javascript
const fruits = ['яблоко', 'банан', 'апельсин'];
fruits.length; // 3
```

#### Последний элемент

Чтобы получить последний элемент, используем `length - 1`:

```javascript
const fruits = ['яблоко', 'банан', 'апельсин'];
const lastFruit = fruits[fruits.length - 1]; // 'апельсин'
```

Почему `-1`? Потому что индексы начинаются с 0, а `length` считает с 1.

---

## 💡 Примеры

### Пример 1: Создание и доступ

```javascript
const colors = ['красный', 'зеленый', 'синий'];

console.log(colors[0]); // 'красный'
console.log(colors[2]); // 'синий'
console.log(colors.length); // 3
```

### Пример 2: Работа с числами

```javascript
const scores = [85, 92, 78, 95];

const firstScore = scores[0]; // 85
const lastScore = scores[scores.length - 1]; // 95

console.log(`Первая оценка: ${firstScore}`);
console.log(`Последняя оценка: ${lastScore}`);
```

### Пример 3: Пустой массив

```javascript
const emptyArray = [];

console.log(emptyArray.length); // 0
console.log(emptyArray[0]); // undefined (элемента нет)
```

### Пример 4: Изменение элемента

```javascript
const fruits = ['яблоко', 'банан', 'апельсин'];

fruits[1] = 'груша'; // Заменяем 'банан' на 'груша'

console.log(fruits); // ['яблоко', 'груша', 'апельсин']
```

---

## ✏️ Задание

### Описание

Напишите две функции для работы с массивами:

1. **`getLastElement(arr)`** — возвращает последний элемент массива. Для пустого массива — `undefined`.

2. **`getElementAt(arr, index)`** — возвращает элемент массива по индексу. Поддерживает **отрицательные индексы**: `-1` — последний элемент, `-2` — предпоследний и т.д. Если индекс выходит за границы — `undefined`.

### Требования

**getLastElement:**
- [ ] Принимает один параметр — массив
- [ ] Возвращает последний элемент массива
- [ ] Для пустого массива — `undefined`
- [ ] Используйте свойство `length` и индексацию

**getElementAt:**
- [ ] Принимает массив и числовой индекс
- [ ] Для положительного индекса — возвращает элемент по этому индексу
- [ ] Для отрицательного индекса — считает с конца (`-1` = последний, `-2` = предпоследний)
- [ ] При выходе за границы массива — `undefined`

### Примеры использования

```javascript
// getLastElement
getLastElement([1, 2, 3, 4, 5]); // 5
getLastElement(['a', 'b', 'c']); // 'c'
getLastElement([42]); // 42
getLastElement([]); // undefined

// getElementAt
getElementAt([10, 20, 30], 0);   // 10
getElementAt([10, 20, 30], 2);   // 30
getElementAt([10, 20, 30], -1);  // 30 (последний)
getElementAt([10, 20, 30], -2);  // 20 (предпоследний)
getElementAt([10, 20, 30], 5);   // undefined (за границами)
getElementAt([], 0);             // undefined
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Для `getLastElement`: как связаны длина массива и индекс последнего элемента? Вспомните, что индексация начинается с нуля.

Для `getElementAt`: что произойдёт, если к длине массива прибавить отрицательное число? Подумайте, как превратить отрицательный индекс в обычный положительный.

</details>

<details>
<summary>Подсказка 2: Структура</summary>

`getLastElement` — это одно выражение с использованием `length` и простой арифметики.

`getElementAt` — нужна проверка: если индекс отрицательный, вычислите реальный индекс через `length`. Например, при `index = -1` и `length = 3` реальный индекс будет `3 + (-1) = 2`.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

Для `getLastElement`: индекс последнего элемента всегда на 1 меньше длины массива. Обращение к несуществующему индексу автоматически вернёт `undefined`.

Для `getElementAt`: проверьте знак индекса через `if`. Если он отрицательный — прибавьте его к длине массива (`length + index`), чтобы получить позицию с конца. Если положительный — используйте как обычный индекс. JavaScript сам вернёт `undefined` при выходе за границы.

</details>

---

## 🧪 Как проверить решение

1. Запустите тесты для этого модуля:
   ```bash
   npx vitest module-03/index.spec.js
   ```

2. Или запустите UI для визуальной проверки:
   ```bash
   npm run test:ui
   ```

---

## 📖 Дополнительные материалы

- [MDN: Массивы](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array)
- [JavaScript.info: Массивы](https://learn.javascript.ru/array)
- [MDN: Array.length](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array/length)

---

## ❓ Частые вопросы

**Q: Почему индексы начинаются с 0, а не с 1?**
A: Это историческая особенность многих языков программирования. Индекс 0 означает "нет смещения от начала массива", индекс 1 — "смещение на 1 элемент" и т.д.

**Q: Что произойдет, если обратиться к несуществующему индексу?**
A: JavaScript вернет `undefined`. Например, `[1, 2, 3][10]` вернет `undefined`.

**Q: Можно ли хранить в массиве элементы разных типов?**
A: Да! В JavaScript массив может содержать что угодно: `[1, 'hello', true, null]`. Но обычно хранят однотипные данные для удобства.

**Q: Почему `length - 1` для последнего элемента?**
A: Если в массиве 5 элементов, `length` равен 5, но индексы: 0, 1, 2, 3, 4. Последний индекс всегда на 1 меньше длины.

**Q: Можно ли изменить элемент массива после создания?**
A: Да, даже если массив объявлен через `const`. `const` защищает только от переприсваивания самой переменной, но не от изменения содержимого массива.

---

## 🎓 Что дальше?

После выполнения этого модуля переходите к **Модуль 4: Циклы (for, while)**, где вы научитесь автоматизировать повторяющиеся действия и перебирать массивы.
