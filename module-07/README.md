# Модуль 7: Объекты (создание, доступ к свойствам)

## 🎯 Цель модуля

После прохождения модуля вы научитесь создавать объекты, получать доступ к их свойствам и изменять их.

---

## 📚 Теория

### Что такое объект?

**Объект** — это коллекция свойств, где каждое свойство имеет имя (ключ) и значение. Если массив — это список элементов по порядку, то объект — это набор именованных значений.

```javascript
const user = {
  name: 'Иван',
  age: 25,
  city: 'Москва'
};
```

### Зачем нужны объекты?

Объекты позволяют группировать связанные данные:
- Данные пользователя (имя, email, возраст)
- Товар в магазине (название, цена, количество)
- Настройки приложения (тема, язык, уведомления)

### Создание объекта

```javascript
// Литеральная нотация (самый частый способ)
const car = {
  brand: 'Toyota',
  year: 2020,
  color: 'красный'
};

// Пустой объект
const empty = {};
```

### Доступ к свойствам

#### Через точку (dot notation)

```javascript
const user = { name: 'Иван', age: 25 };

console.log(user.name); // 'Иван'
console.log(user.age);  // 25
```

#### Через квадратные скобки (bracket notation)

```javascript
const user = { name: 'Иван', age: 25 };

console.log(user['name']); // 'Иван'
console.log(user['age']);  // 25

// Полезно когда ключ в переменной
const key = 'name';
console.log(user[key]); // 'Иван'
```

### Изменение и добавление свойств

```javascript
const user = { name: 'Иван' };

// Изменение существующего свойства
user.name = 'Петр';

// Добавление нового свойства
user.age = 30;
user.city = 'Москва';

console.log(user); // { name: 'Петр', age: 30, city: 'Москва' }
```

### Сокращённая запись свойств

Если имя переменной совпадает с именем свойства:

```javascript
const name = 'Иван';
const age = 25;

// Длинная запись
const user1 = { name: name, age: age };

// Сокращённая запись (ES6)
const user2 = { name, age };

// Оба варианта создают { name: 'Иван', age: 25 }
```

---

## 💡 Примеры

### Пример 1: Создание объекта пользователя

```javascript
const user = {
  name: 'Мария',
  age: 28,
  email: 'maria@example.com'
};

console.log(user.name);  // 'Мария'
console.log(user.email); // 'maria@example.com'
```

### Пример 2: Динамический доступ к свойствам

```javascript
const product = {
  name: 'Ноутбук',
  price: 50000,
  inStock: true
};

function getProperty(obj, key) {
  return obj[key];
}

getProperty(product, 'name');  // 'Ноутбук'
getProperty(product, 'price'); // 50000
```

### Пример 3: Функция создания объекта

```javascript
function createProduct(name, price) {
  return {
    name: name,
    price: price,
    inStock: true
  };
}

const laptop = createProduct('Ноутбук', 50000);
// { name: 'Ноутбук', price: 50000, inStock: true }
```

---

## ✏️ Задание

### Описание

Создайте две функции для работы с объектами пользователей:

1. **`createUser(name, age)`** — создаёт и возвращает объект пользователя
2. **`formatUser(user)`** — возвращает строку с информацией о пользователе

### Требования

- [ ] `createUser` принимает имя и возраст, возвращает объект `{ name, age }`
- [ ] `formatUser` принимает объект пользователя
- [ ] `formatUser` возвращает строку в формате `"Имя: X, Возраст: Y"`
- [ ] Используйте доступ к свойствам через точку

### Примеры использования

```javascript
// createUser
const user1 = createUser('Иван', 25);
console.log(user1); // { name: 'Иван', age: 25 }

const user2 = createUser('Мария', 30);
console.log(user2); // { name: 'Мария', age: 30 }

// formatUser
formatUser({ name: 'Иван', age: 25 });   // "Имя: Иван, Возраст: 25"
formatUser({ name: 'Мария', age: 30 });  // "Имя: Мария, Возраст: 30"

// Вместе
const user = createUser('Петр', 35);
formatUser(user); // "Имя: Петр, Возраст: 35"
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Как создать объект с заданными свойствами и вернуть его из функции? Как получить доступ к свойству объекта и подставить его значение в строку?

</details>

<details>
<summary>Подсказка 2: Структура</summary>

Первая функция должна создать объект с двумя свойствами и вернуть его. Можно воспользоваться сокращённой записью свойств (shorthand), когда имя параметра совпадает с именем ключа. Вторая функция получает объект, читает его свойства через точку и подставляет их в шаблонную строку (template literal).

</details>

<details>
<summary>Подсказка 3: Подход</summary>

В `createUser` верните объект с ключами `name` и `age`, значения которых берутся из параметров функции. В `formatUser` обратитесь к свойствам переданного объекта (`user.name` и `user.age`) и подставьте их в шаблонную строку формата `"Имя: ..., Возраст: ..."`. Убедитесь, что строка точно соответствует требуемому формату, включая пробелы и запятую.

</details>

---

## 🧪 Как проверить решение

1. Запустите тесты для этого модуля:
   ```bash
   npx vitest module-07/index.spec.js
   ```

2. Или запустите UI для визуальной проверки:
   ```bash
   npm run test:ui
   ```

---

## 📖 Дополнительные материалы

- [MDN: Работа с объектами](https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Working_with_Objects)
- [JavaScript.info: Объекты](https://learn.javascript.ru/object)
- [JavaScript.info: Доступ к свойствам](https://learn.javascript.ru/object#kvadratnye-skobki)

---

## ❓ Частые вопросы

**Q: Когда использовать точку, а когда квадратные скобки?**
A: Точку — когда знаете имя свойства заранее (`user.name`). Квадратные скобки — когда имя в переменной (`user[key]`) или содержит спецсимволы.

**Q: Можно ли использовать числа как ключи?**
A: Да, но они автоматически преобразуются в строки: `obj[1]` это то же что `obj['1']`.

**Q: Что будет если обратиться к несуществующему свойству?**
A: Вернётся `undefined`, ошибки не будет.

**Q: Можно ли удалить свойство из объекта?**
A: Да, с помощью `delete obj.property`, но это редко нужно.

---

## 🎓 Что дальше?

После выполнения этого модуля переходите к **Модуль 8: Debugging (Отладка)**, где вы научитесь находить и исправлять ошибки в коде.
