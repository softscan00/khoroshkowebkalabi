# Модуль 10: Set, Map и хэш-функции

## 🎯 Цель модуля

После прохождения модуля вы узнаете о структурах данных Set и Map, поймёте что такое хэш-функции и почему эти структуры работают быстрее массивов для поиска.

---

## 📚 Теория

### Проблема поиска в массиве

Представьте, что у вас массив из 1 000 000 элементов и нужно проверить, есть ли там число 42:

```javascript
const arr = [1, 2, 3, /* ...миллион элементов... */];
arr.includes(42); // O(n) — проверяет каждый элемент по очереди
```

В худшем случае нужно проверить ВСЕ элементы. Это называется **O(n)** — линейная сложность.

### Что такое хэш-функция?

**Хэш-функция** — это функция, которая превращает любые данные в число (хэш).

```
"apple"  → 97 + 112 + 112 + 108 + 101 = 530
"banana" → 98 + 97 + 110 + 97 + 110 + 97 = 609
```

Ключевые свойства:
- Одинаковые данные → одинаковый хэш
- Быстро вычисляется
- Разные данные обычно дают разные хэши

### Как это ускоряет поиск?

Вместо хранения элементов в ряд, мы используем хэш как "адрес":

```
Хэш "apple" = 530
Кладём "apple" в ячейку 530

Поиск "apple":
1. Вычисляем хэш = 530
2. Смотрим в ячейку 530
3. Нашли!
```

Это **O(1)** — константная сложность. Не важно сколько элементов, поиск занимает одинаковое время!

### Set — множество уникальных значений

**Set** хранит только уникальные значения и использует хэширование для быстрого поиска.

```javascript
const set = new Set();

// Добавление
set.add('apple');
set.add('banana');
set.add('apple'); // Игнорируется — уже есть

console.log(set); // Set { 'apple', 'banana' }

// Проверка наличия — O(1), мгновенно!
set.has('apple');  // true
set.has('orange'); // false

// Удаление
set.delete('apple');

// Размер
set.size; // 1
```

#### Создание Set из массива

```javascript
const arr = [1, 2, 2, 3, 3, 3];
const unique = new Set(arr);
console.log(unique); // Set { 1, 2, 3 }

// Обратно в массив
const uniqueArr = [...unique]; // [1, 2, 3]
```

### Map — словарь ключ-значение

**Map** похож на объект, но ключом может быть что угодно (не только строки), и поиск по ключу — O(1).

```javascript
const map = new Map();

// Добавление
map.set('name', 'Иван');
map.set('age', 25);
map.set(42, 'ответ');  // Ключ — число
map.set(true, 'да');   // Ключ — boolean

// Получение значения — O(1)!
map.get('name'); // 'Иван'
map.get(42);     // 'ответ'

// Проверка наличия ключа
map.has('name'); // true

// Удаление
map.delete('age');

// Размер
map.size; // 3
```

### Сравнение: массив vs Set vs Map

| Операция | Массив | Set | Map |
|----------|--------|-----|-----|
| Поиск элемента | O(n) | O(1) | O(1) |
| Добавление | O(1) | O(1) | O(1) |
| Удаление по значению | O(n) | O(1) | O(1) |
| Дубликаты | Да | Нет | Нет (ключи) |

---

## 💡 Примеры

### Пример 1: Работа с Set

```javascript
const visitedPages = new Set();

visitedPages.add('/home');
visitedPages.add('/about');
visitedPages.add('/home'); // Игнорируется — уже есть

console.log(visitedPages.size); // 2
console.log(visitedPages.has('/home')); // true
console.log(visitedPages.has('/contact')); // false
```

### Пример 2: Работа с Map

```javascript
const userScores = new Map();

userScores.set('Иван', 100);
userScores.set('Мария', 150);
userScores.set('Иван', 120); // Перезаписывает старое значение

console.log(userScores.get('Иван')); // 120
console.log(userScores.get('Петр')); // undefined
console.log(userScores.size); // 2
```

### Пример 3: Быстрая проверка наличия

```javascript
// Медленно — O(n) на каждую проверку
const bannedUsers = ['user1', 'user2', 'user3'];
bannedUsers.includes('user2'); // Проверяет весь массив

// Быстро — O(1) на каждую проверку
const bannedSet = new Set(['user1', 'user2', 'user3']);
bannedSet.has('user2'); // Мгновенно!
```

### Пример 4: Перебор Map в цикле

```javascript
const prices = new Map();
prices.set('яблоко', 50);
prices.set('банан', 30);

for (const [product, price] of prices) {
  console.log(`${product}: ${price} руб.`);
}
// яблоко: 50 руб.
// банан: 30 руб.
```

---

## ✏️ Задание

### Описание

Создайте две функции, используя Set и Map:

1. **`getUnique(arr)`** — возвращает массив уникальных элементов
2. **`countItems(arr)`** — возвращает Map с количеством каждого элемента

### Требования

**getUnique:**
- [ ] Принимает массив
- [ ] Возвращает новый массив без дубликатов
- [ ] Сохраняет порядок первого появления элементов
- [ ] Использует Set

**countItems:**
- [ ] Принимает массив
- [ ] Возвращает Map, где ключ — элемент, значение — количество
- [ ] Использует Map

### Примеры использования

```javascript
// getUnique
getUnique([1, 2, 2, 3, 1, 4]);     // [1, 2, 3, 4]
getUnique(['a', 'b', 'a', 'c']);   // ['a', 'b', 'c']
getUnique([]);                      // []

// countItems
countItems(['a', 'b', 'a', 'a']);
// Map { 'a' => 3, 'b' => 1 }

countItems([1, 2, 1, 3, 2, 1]);
// Map { 1 => 3, 2 => 2, 3 => 1 }
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Для `getUnique`: какая структура данных автоматически удаляет дубликаты? Как преобразовать её обратно в массив?

Для `countItems`: какая структура данных хранит пары «ключ-значение» и позволяет обновлять значение по ключу?

</details>

<details>
<summary>Подсказка 2: Структура</summary>

Для `getUnique`: вспомните, что `Set` можно создать прямо из массива, передав его в конструктор. А чтобы превратить Set обратно в массив, существует spread-оператор (`...`).

Для `countItems`: создайте пустую `Map` и пройдитесь по массиву циклом. На каждой итерации проверяйте, есть ли уже элемент в Map — если да, увеличивайте счётчик, если нет — устанавливайте начальное значение.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

Функция `getUnique` решается в одну строку: создайте `Set` из входного массива и сразу разверните его обратно в массив.

Функция `countItems` требует перебора массива через `for...of`. Для каждого элемента используйте `map.get()`, чтобы получить текущее значение счётчика. Учтите, что `map.get()` возвращает `undefined` для несуществующих ключей — вам нужно обработать этот случай, чтобы начать отсчёт с нуля. Затем обновите значение через `map.set()`.

</details>

---

## 🧪 Как проверить решение

1. Запустите тесты для этого модуля:
   ```bash
   npx vitest module-10/index.spec.js
   ```

2. Или запустите UI для визуальной проверки:
   ```bash
   npm run test:ui
   ```

---

## 📖 Дополнительные материалы

- [MDN: Set](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Set)
- [MDN: Map](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Map)
- [JavaScript.info: Map и Set](https://learn.javascript.ru/map-set)
- [Хэш-таблицы (Wikipedia)](https://ru.wikipedia.org/wiki/Хеш-таблица)

---

## ❓ Частые вопросы

**Q: Чем Map лучше обычного объекта?**
A: Map принимает любые ключи (не только строки), сохраняет порядок вставки, имеет свойство `.size` и методы `.has()`, `.delete()`. Для данных — используйте Map, для структур с методами — объект.

**Q: Что если два значения дают одинаковый хэш?**
A: Это называется "коллизия". Set и Map умеют их обрабатывать, храня несколько значений в одной ячейке. На практике это редко влияет на производительность.

**Q: Можно ли перебрать Set или Map в цикле?**
A: Да! Используйте `for...of`:
```javascript
for (const item of set) { ... }
for (const [key, value] of map) { ... }
```

**Q: Set хранит только примитивы?**
A: Нет, Set может хранить объекты, но сравнивает их по ссылке, не по содержимому:
```javascript
const set = new Set();
set.add({ a: 1 });
set.add({ a: 1 }); // Это ДРУГОЙ объект
set.size; // 2
```

---

## 🎓 Что дальше?

После выполнения этого модуля переходите к **Модуль 11: Git**, где вы познакомитесь с системой контроля версий.
