# Модуль 2: Условия и операторы сравнения

## 🎯 Цель модуля

Научиться принимать решения в коде с помощью условных конструкций `if`, `else if`, `else`, познакомиться с операторами сравнения (`===`, `!==`, `>`, `<`, `>=`, `<=`) и логическими операторами (`&&`, `||`, `!`).

---

## 📚 Теория

### Что такое условия?

**Условия** (conditionals) позволяют программе принимать решения и выполнять разный код в зависимости от ситуации.

Представьте светофор: если свет зеленый — идти, если красный — стоять. Это условие!

```javascript
if (светЗеленый) {
    идти();
} else {
    стоять();
}
```

### Зачем это нужно?

В реальных приложениях постоянно нужно проверять условия:
- Проверить возраст пользователя перед показом контента
- Показать разные сообщения в зависимости от статуса заказа
- Проверить правильность введенного пароля
- Изменить интерфейс в зависимости от роли пользователя

### Как это работает?

#### 1. Оператор `if`

Базовая конструкция: "Если условие истинно, выполни код"

```javascript
const age = 18;

if (age >= 18) {
    console.log("Вы совершеннолетний");
}
```

#### 2. Оператор `if...else`

"Если условие истинно — одно, иначе — другое"

```javascript
const age = 16;

if (age >= 18) {
    console.log("Вы можете голосовать");
} else {
    console.log("Вы пока не можете голосовать");
}
```

#### 3. Оператор `if...else if...else`

"Проверить несколько условий по очереди"

```javascript
const score = 85;

if (score >= 90) {
    console.log("Отлично!");
} else if (score >= 70) {
    console.log("Хорошо!");
} else if (score >= 50) {
    console.log("Удовлетворительно");
} else {
    console.log("Нужно подтянуть знания");
}
```

#### 4. Операторы сравнения

| Оператор | Описание | Пример | Результат |
|----------|----------|---------|-----------|
| `===` | Строгое равенство | `5 === 5` | `true` |
| `!==` | Строгое неравенство | `5 !== 3` | `true` |
| `>` | Больше | `10 > 5` | `true` |
| `<` | Меньше | `3 < 8` | `true` |
| `>=` | Больше или равно | `5 >= 5` | `true` |
| `<=` | Меньше или равно | `4 <= 7` | `true` |

**Важно**: Используйте `===` вместо `==`! Строгое равенство проверяет и значение, и тип:

```javascript
5 == "5"   // true (нестрогое, приводит типы)
5 === "5"  // false (строгое, разные типы)
```

#### 5. Логические операторы

**`&&` (AND, "И")** — истина, если ОБА условия истинны:
```javascript
const age = 20;
const hasLicense = true;

if (age >= 18 && hasLicense) {
    console.log("Можете водить машину");
}
```

**`||` (OR, "ИЛИ")** — истина, если ХОТЯ БЫ ОДНО условие истинно:
```javascript
const isWeekend = true;
const isHoliday = false;

if (isWeekend || isHoliday) {
    console.log("Можно отдыхать!");
}
```

**`!` (NOT, "НЕ")** — инвертирует значение:
```javascript
const isRaining = false;

if (!isRaining) {
    console.log("Можно идти гулять");
}
```

#### 6. Truthy и Falsy значения

В JavaScript некоторые значения считаются "ложными" (falsy):
- `false`
- `0`
- `""` (пустая строка)
- `null`
- `undefined`
- `NaN`

Все остальное — "истинное" (truthy):
```javascript
if ("привет") { // true, непустая строка
    console.log("Это выполнится");
}

if (0) { // false
    console.log("Это НЕ выполнится");
}
```

---

## 💡 Примеры

### Пример 1: Проверка возраста

```javascript
function canVote(age) {
    if (age >= 18) {
        return "Вы можете голосовать";
    } else {
        return "Вы не можете голосовать";
    }
}

console.log(canVote(20)); // "Вы можете голосовать"
console.log(canVote(16)); // "Вы не можете голосовать"
```

**Объяснение**: Проверяем возраст и возвращаем разные строки

### Пример 2: Оценка за экзамен

```javascript
function getGrade(score) {
    if (score >= 90) {
        return "A";
    } else if (score >= 80) {
        return "B";
    } else if (score >= 70) {
        return "C";
    } else if (score >= 60) {
        return "D";
    } else {
        return "F";
    }
}

console.log(getGrade(95)); // "A"
console.log(getGrade(75)); // "C"
console.log(getGrade(55)); // "F"
```

**Объяснение**: Последовательно проверяем диапазоны баллов

### Пример 3: Логические операторы

```javascript
function canBuyAlcohol(age, hasID) {
    if (age >= 21 && hasID) {
        return "Можно продать";
    } else {
        return "Нельзя продать";
    }
}

console.log(canBuyAlcohol(25, true));  // "Можно продать"
console.log(canBuyAlcohol(25, false)); // "Нельзя продать"
console.log(canBuyAlcohol(18, true));  // "Нельзя продать"
```

**Объяснение**: Проверяем ДВА условия одновременно (возраст И наличие документа)

### Пример 4: Тернарный оператор (краткая форма if-else)

```javascript
const age = 20;
const message = age >= 18 ? "Совершеннолетний" : "Несовершеннолетний";

console.log(message); // "Совершеннолетний"
```

**Объяснение**: `условие ? значениеЕслиTrue : значениеЕслиFalse` — короткая форма if-else

---

## ✏️ Задание

### Описание

Создайте функцию `checkAccess`, которая принимает два параметра: возраст пользователя (`age`) и булево значение "является ли премиум-пользователем" (`isPremium`). Функция должна вернуть строку с результатом проверки доступа.

### Требования

- [ ] Функция должна называться `checkAccess`
- [ ] Функция принимает 2 параметра: `age` (число) и `isPremium` (boolean)
- [ ] Если возраст меньше 18 — вернуть `"Доступ запрещен: возраст менее 18 лет"`
- [ ] Если возраст >= 18 и пользователь премиум — вернуть `"Полный доступ"`
- [ ] Если возраст >= 18, но НЕ премиум — вернуть `"Базовый доступ"`

### Примеры использования

```javascript
checkAccess(16, false);
// Возвращает: "Доступ запрещен: возраст менее 18 лет"

checkAccess(16, true);
// Возвращает: "Доступ запрещен: возраст менее 18 лет"

checkAccess(25, true);
// Возвращает: "Полный доступ"

checkAccess(25, false);
// Возвращает: "Базовый доступ"
```

---

## 💭 Подсказки

<details>
<summary>Подсказка 1: Направление</summary>

Подумайте, какое условие нужно проверить в первую очередь, чтобы сразу «отсечь» пользователей, которым доступ запрещён безоговорочно. Какое свойство является блокирующим, независимо от других параметров?

</details>

<details>
<summary>Подсказка 2: Структура</summary>

Задачу можно решить последовательными проверками: сначала «отсеять» запрещённый случай и сразу вернуть результат, а затем среди оставшихся разделить пользователей по второму параметру. Такой приём называется **early return** — вы выходите из функции как можно раньше, если условие не выполнено.

</details>

<details>
<summary>Подсказка 3: Подход</summary>

Функция должна содержать два уровня проверки. Первым делом сравните `age` с граничным значением (18) — если возраст меньше, сразу верните строку об отказе в доступе. Если эта проверка пройдена (то есть пользователю 18 или больше), проверьте значение `isPremium` и верните соответствующую строку: одну для премиум-пользователей, другую для обычных.

</details>

---

## 🧪 Как проверить решение

1. Запустите тесты для этого модуля:
   ```bash
   npx vitest module-02/index.spec.js
   ```

2. Или запустите UI для визуальной проверки:
   ```bash
   npm run test:ui
   ```

3. Все 4 теста должны быть зелеными ✅

---

## 📖 Дополнительные материалы

- [MDN: if...else](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/if...else)
- [MDN: Операторы сравнения](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Operators/Comparison_Operators)
- [MDN: Логические операторы](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Operators/Logical_Operators)
- [Learn.javascript.ru: Условное ветвление](https://learn.javascript.ru/ifelse)
- [Learn.javascript.ru: Логические операторы](https://learn.javascript.ru/logical-operators)

---

## ❓ Частые вопросы

**Q: В чем разница между `==` и `===`?**
A: `===` (строгое равенство) проверяет тип И значение, `==` (нестрогое) приводит типы. ВСЕГДА используйте `===`.
```javascript
5 == "5"   // true (плохо!)
5 === "5"  // false (хорошо!)
```

**Q: Что вернет `if ("0")`?**
A: True! Непустая строка — это truthy значение, даже если это строка "0".

**Q: Можно ли писать `if` без фигурных скобок?**
A: Можно, если одна строка кода: `if (x > 5) return true;`. Но лучше ВСЕГДА использовать `{}` для читаемости.

**Q: В каком порядке проверяются условия в `if...else if`?**
A: Сверху вниз. Как только одно условие истинно — остальные не проверяются.

---

## 🎓 Что дальше?

После выполнения этого модуля переходите к **Модуль 3: Массивы**, где вы научитесь создавать массивы, получать доступ к элементам по индексу и работать со свойством `length`.
